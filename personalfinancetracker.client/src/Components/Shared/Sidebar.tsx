const Sidebar = () => {
    return (
        <aside className="sidebar">
            <button className="primary-btn">Dashboard</button>
            <button className="primary-btn">Transactions</button>
            <button className="primary-btn">Budgets</button>
            <button className="primary-btn">Categories</button>
            <button className="primary-btn">Reports</button>
        </aside>
    );
};

export default Sidebar;